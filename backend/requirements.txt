# ============================================================
# ZAMIN X v2 — requirements.txt
# Python 3.11+
# ============================================================

# --- Core API ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.0
python-multipart==0.0.9
python-dotenv==1.0.1

# --- Database (SQLite for dev) ---
sqlalchemy==2.0.30
aiosqlite==0.20.0

# --- HTTP Client ---
httpx==0.27.0

# --- AI (Groq via HTTP, no heavy deps) ---
# Groq accessed via httpx REST calls — no SDK needed

# --- Utilities ---
pyyaml==6.0.1
tenacity==8.3.0
beautifulsoup4==4.12.3
lxml==5.2.2
